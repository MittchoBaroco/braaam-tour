<%= form_with(model: admin_manager, local: true) do |form| %>
  <% if admin_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_manager.errors.count, "error") %> prohibited this admin_manager from being saved:</h2>

      <ul>
      <% admin_manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
